# 2026-03-03

## Durable updates

- Isaac clarified a workflow preference: when he says **「開始改」** (or even just **「ok」**), execute directly without extra confirmation.
- Workspace/repo strategy changed:
  - `~/.openclaw/workspace` is now treated as the main working repo scope.
  - `workspace` remote should point to `git@github.com:axxdeveloper/open-claw-bot-codes.git` (with HTTPS fallback used for pushes when needed).
  - `~/.openclaw/workspace/shooeugenesea.github.io` remains an independent nested repo and should keep pushing to `git@github.com:axxdeveloper/shooeugenesea.github.io.git` (to preserve blog Actions publish flow).
- CRE auth/login decisions:
  - Login UX changed to password-only entry (no email field shown to user).
  - Two password routes are expected: primary `0910301562` and test `0912505345`, mapped to different internal users.
  - Isaac explicitly rejected showing plaintext passwords on the login page; all visible password hints were removed.
- CRE deployment/runtime fixes completed:
  - Removed legacy `cre-building-lease-os/frontend/` tree and kept root Next.js app (`app/`, `components/`, `lib/`) as primary UI source.
  - Added root Dockerfile and updated compose build context to root.
  - Added auth DB wiring and Prisma runtime compatibility fixes for container (`DATABASE_URL`, Prisma binary target for linux-musl-openssl-3.0.x, `prisma generate` in Docker build).
- Data import outcome (tmp-tenant.xlsx):
  - Confirmed import rules with Isaac: target building = 宏盛國際金融中心; merge same tenant across floors; convert ROC date to Gregorian when parseable; import all sheets.
  - Import completed with batch id `cmma7pqd50001rkht027on10q`.
- UI direction confirmations from Isaac:
  - Buildings overview should match dashboard/table style reference (KPI cards + filters + searchable list).
  - Tenant detail page should follow card-based profile/contact/documents layout reference.
  - Repairs page should follow Kanban + right detail panel reference.

## Notes for future behavior

- Avoid internal jargon in user-facing summaries (Isaac called this out as noise).
- Treat delayed/queued old messages carefully; Isaac prefers current-context responses only.
