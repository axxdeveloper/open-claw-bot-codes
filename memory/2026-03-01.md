
## CRE project durable updates (2026-03-01)

- Deployment/access:
  - App is running via Docker containers (`cre-backend`, `cre-frontend`, `cre-nginx`) on host.
  - LAN access confirmed at `http://192.168.1.101:3000` (frontend) and `http://192.168.1.101:8080` (API).
  - Internet access currently uses HTTP on `:8080`; Cloudflare Quick Tunnel was created for temporary HTTPS access (ephemeral `trycloudflare` URL).
- Security baseline applied before internet exposure:
  - Added login gate page and auth cookie middleware/proxy behavior.
  - Added nginx reverse proxy profile with basic anti-scan/rate-limit headers/rules.
  - Fixed proxied login redirect bug that produced client-side exception (`0.0.0.0` redirect issue).
- Data import state:
  - Re-imported official Excel data multiple times after environment resets.
  - Current primary building code remains `HONGSHENG` (宏盛國際金融中心), with occupancy data normalized from DRAFT to ACTIVE for official imported records.
  - User explicitly expects official imported occupants to be treated as completed/active, not draft.
- UX/product direction decisions from user:
  - Strong preference for data-first UI (tables/details first), not KPI-first dashboards.
  - Remove low-value/abstract blocks: "今日營運重點", unnecessary stats cards, and generic management cards.
  - Navigation should be simplified for mobile: controls collapsed behind a top-right menu.
  - Show full floor directory (including empty floors), with per-floor management actions.
  - Building page should directly show floor/company directory and facilities, similar to real building directory mental model.
  - Vendor management should be an independent module; repairs requests should assign from maintained vendor list.
- Docs/user enablement:
  - User requested practical user guide with screenshots; doc tabs were consolidated to a single tab name: `CRE 使用者手冊`.
  - User expects all shared URLs to be clickable.
- Process requirement reinforcement:
  - User requires true progress reporting via cron (not inferred status), with evidence from real git/process state.
