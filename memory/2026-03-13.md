# 2026-03-13

## Notes
- Confirmed persistent preference: when Isaac requests "gpt-5.4 xhigh", execute via Codex CLI route (`codex exec` + `model_reasoning_effort=xhigh`) rather than relying only on agent/cron `thinking`.
- Verified MEMORY.md already contains this rule under "Working agreements with Isaac".
- Follow-up fix completed after a prior edit-path mismatch warning.
