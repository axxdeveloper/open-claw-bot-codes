# 2026-03-10

## Durable updates

- User requested backend knowledge cadence change:
  - from hourly to every 2 hours
  - skip midnight to 08:00 (Asia/Taipei)
  - applied cron schedule: `0 8-23/2 * * *` for `backend-hourly-knowledge`.

- Added voice delivery requirement for backend/economy outputs:
  - backend skill updated to require `語音稿（TypeScript）` section.
  - market brief generation script updated to require `語音稿（TypeScript）` section.

- Added new voice cron jobs:
  - `backend-hourly-knowledge-voice` (`b97b844f-aec6-440d-af87-55b09f63ac44`) schedule `5 8-23/2 * * *`
  - `tw-market-close-voice` (`8c12e65f-cf17-437d-b314-090bd6231df6`) schedule `10 15 * * 1-5`
  - `us-market-close-voice` (`68d1c0ee-9918-4a87-a4b8-ec76ca0c7967`) schedule `40 6 * * 1-5`.

- Fixed timeout issue for daily macro research:
  - `macro-post-daily-research-0700` timeout set to `3600s` (was effectively hitting default limit and got SIGTERM around ~15 min).

- Updated CLI release watch behavior:
  - now reports **stable** versions explicitly (plus optional next/preview reference).
  - cron `cli-release-watch-daily` kept daily at 09:00.

- Reliability fix for voice cron execution:
  - voice cron prompts hardened to force tool flow: draft voice script -> call `tts` -> reply `NO_REPLY`.
  - increased timeouts to avoid cutoff:
    - backend voice `900s`
    - tw/us market voice `1800s` each.

- Ongoing environment note:
  - intermittent `gateway timeout after 30000ms (ws://127.0.0.1:18789)` still occurs; manual re-run sometimes needed.
