# HEARTBEAT.md

- 若以下皆無新狀況，回 `HEARTBEAT_OK`
- gog-based 自動化先暫停；不要主動跑 `gog`，也不要重複回報 gog auth blocker
- 每 15 分鐘輪詢一次：執行中 tasks、OpenClaw sessions、cron 長任務、article-video-publisher 狀態、YouTube Community source-intro cron 狀態
- 小舟原始來源導讀走 YouTube Community Posts；heartbeat 只監控它是否卡住、重複發文或遇到 YouTube Studio/account/Posts/composer/browser blocker，不另外加發。該 cron 檢查最近選題選中的技術、AI、backend/software、工程 operator 訪談原始來源：`reports/article-video-publisher/**/source.json` / `final-report.md` / `youtube/upload-result.json` / source-watch/source-discovery reports，查 `reports/youtube-community-source-intros/posted-sources.jsonl` 避免重複；若找到有明確來源內容、通過品質/安全 gate、且尚未介紹的來源，可依 `skills/youtube-community-publisher/SKILL.md` 發一則 source-first 中文導讀並記錄 YouTube Community post URL。導讀不能只開題，必須先說這是什麼來源，再交代 1-2 個來源支持的結論、限制或含義；takeaway 要用直述資訊，不要用「先問...」這種問題句代替答案，也不要把來源硬改成導入/驗收/特定角色用途。原始來源 URL 是主連結；沒有 TodayShip 影片也可以發；若同題已有 TodayShip 公開影片，只在文末加小附註。保守節奏最多一天 3 則、至少間隔 2 小時；若有 active 產片/上傳/recovery/browser blocker，先 no-op 並回報原因。
- Gateway restart 後第一件事：檢查 cron runs / sessions 是否有 `cron: job interrupted by gateway restart`、`gateway restart`、`aborted` 或沒有 final report 的長任務。若任務是可恢復的 configured cron/skill workflow，先確認同 job 沒有仍在 running，再用 `cron run` 或本地 artifacts 接續；不要等 Isaac 說 Resume。若任務可能造成重複 Public 發布，先檢查 `reports/**/status.md`、`youtube/upload-result.json`、公開 URL 與上傳狀態，再決定續跑或只補 bookkeeping。
- 若 artifact 的 `status.md` 標記 `paused_by_isaac` 或 `do_not_resume_without_explicit_request: true`，不得由 heartbeat/cron 自動恢復；只有 Isaac 明確說 resume / 繼續才可接續。
- task 重試到期、長任務有結果、影片發布流程進入可見階段或卡住時主動回報
- Recovered/resumed 影片一旦 published 或修好已公開影片 bookkeeping，必須立即發可見 final report，列出 YouTube URL、原始來源 URL、發布時間、長度、review/public-page verification、TTS/audio 檢查與 cleanup 狀態；不要等下一個 heartbeat/cron。
- 若後續 cron/heartbeat skip/no-op 的原因是 active/recovered/just-completed 影片工作流，回報前先查 `status.md`、`youtube/upload-result.json` 與公開 URL；若已 Public，skip 訊息必須帶上既有影片 URL 與狀態，不可只說「已有任務進行中」讓 Isaac 看起來像沒有產出。
- 若後續 cron/heartbeat 想因 pending artifact blocker 而 skip，先把它當 incident 做 recovery triage，不可只報 skip：列出 pending artifact、查 active sessions/processes、檢查 `status.md` / `source.json` / `review/review.md` / `video/build-result.txt` / `youtube/metadata.json` / `youtube/upload-result.json` / 公開 URL；已 Public 就補 bookkeeping/final report，`review_passed_pending_upload` 且無 upload result 就續 upload，`rendered_pending_review_upload` 就先跑/驗證 review，不能安全續才把 `status.md` 標成 `blocked` 並寫明下一步。這類事件要在 `reports/incidents/YYYY-MM-DD-<slug>.md` 留 incident review，避免 pending 長期擋住新片。
- 若 pending artifact 只是已上傳影片的 YouTube processing/public playback verification（例如 Pixel `published_public_processing_pending_verification` / `youtube_processing_not_ready`，且 metadata/oEmbed 已可見），它只屬於同影片 retry/bookkeeping lane，不得讓一般產片 slot 停止選新題。只有 active render/upload/TTS/Studio、quota、duplicate upload risk，或真正 review/upload-stage recovery 才能擋新片。
- 「影片已 Public 但 Isaac 沒收到 final report」視為 report-miss，要立即補報；這種補報不是新上傳，也要明確避免誤導成第二次發布。
- 明確可執行的工作不要等待 Isaac 回 ok / confirm / 關鍵字；能安全推進就主動推進
- Chrome/CDP/browser profile/OAuth/YouTube Studio/upload 卡住是高優先 blocker，要回報具體錯誤與下一個 retry path
- 若 `browser.status` 或 `curl /json/version` 顯示 CDP alive，但 `browser snapshot/act` 報 `connectOverCDP` timeout，先檢查 Chrome helper CPU / stale renderer，重啟受控 OpenClaw Chrome/profile，並驗證 `browser act` + `browser snapshot`；不要先重啟 gateway
- Playwright/CDP/browser 任務結束前必須回收資源：關掉多餘 tabs、關閉 Playwright page/context/browser、清掉 temp profile/process，並用 `scripts/check_browser_playwright_cleanup.py` 或等效檢查確認沒有 high-CPU Chrome helper / stale renderer 累積
- 若長時間 browser / 產影片 / YouTube Studio 操作後系統變慢、browser attach 變不穩、cron timeout 變多或 Chrome helper CPU 異常，視為系統健康事件：用 Codex/local shell 先 debug，不要盲目重試。檢查 active cron/session、`ps` CPU/記憶體、磁碟、Chrome/Playwright/ffmpeg/node/python/openclaw/codex process、reports/tmp 成長與 cleanup script；優先做精準修復（關多餘 tab/context、停 stale task-owned helper、重啟受控 Chrome profile、清 task-owned temp），確認沒有 active render/upload/TTS/ffmpeg/codex 任務後才終止程序。只有 targeted cleanup 失敗或 scheduler 狀態壞掉時才考慮 restart gateway。
- 若 cron/session 出現 `codex app-server attempt timed out`、`codex app-server turn idle timed out waiting for turn/completed`、`stopReason=aborted`，或 cron 顯示 ok 但 session 只留下進度文字，視為可恢復中斷：先查 artifact 的 `status.md`、`source.json`、`review/review.md`、`video/build-result.txt`、`youtube/upload-result.json` 與公開 URL；已 Public 就只補 bookkeeping/final report，`review_passed_pending_upload` 就續 upload，只有 source/plan 就從下一個缺漏 stage 續做。若同一路徑重複 timeout，改用 `HOME=/Users/openclaw-user CODEX_HOME=/Users/openclaw-user/.codex codex exec ...` 做 xhigh planning/review，再交給本地 deterministic script 產出。
- timeout / aborted / tool relay / browser attach 問題一律先 house keeping 再 retry：查 `cron status/list/runs`、visible sessions、gateway `.err.log/.log`、artifact checkpoint、CPU/記憶體/磁碟、active Chrome/Playwright/ffmpeg/TTS/upload/codex process、browser cleanup。若品質學習或 scout job 正在卡住 production/recovery，先 no-op 或暫停該類 job，不要讓學習任務搶 production 恢復資源。
- 改 cron / 啟用新 slot 後，立刻檢查 `nextRunAt` / 最新 runs；優先用 `cron update` 保留 scheduler state，不要讓 newly enabled slot 因空 state 被當成 due run 立刻補跑
- Browser/YouTube Studio/upload/public-page 驗證截圖預設只存內部，不要批量傳 Telegram；除非 Isaac 明確要求，或 blocker 必須用圖才說得清楚，最多傳一張並附簡短說明
- Google/YouTube/NotebookLM/Slides/Drive 工作若要用 Chrome，先確認 profile email 是 `zwl9999999@gmail.com`；不要用 `profile=user`、`Profile 1`、`Default` 或未驗證 profile 繼續嘗試
- 一般影片排程只能做單一主題；公開來源必須是原始來源 URL，不可把 Isaac Note、PR、report、daily-audio-pack、youtube-pack、HN/source-watch 寫成影片來源；也不要再為影片排程自動發 Isaac Note 文章、commit、branch 或 PR，除非 Isaac 明確指定。例外：每週技術雷達可使用 `reports/weekly-tech-radar/YYYY-MM-DD.md` 作為內部策展稿，但 public metadata/slides/description 要列出被介紹的原始來源 URL，不可把 weekly radar report 或 source-watch 當公開來源。
- YouTube description 的 `文章來源：` 必須獨立一行，下一行放完整 raw `https://...` URL 以支援長片可點擊，並立刻在同一 `文章來源` 區塊加短分行 fallback 讓 domain/slug 在手機也可見；不要再寫成 `文章來源：https://...` 同一行。上傳前必跑 `scripts/lint_youtube_description_source.py` 並要求 `review/description-source-lint.json` PASS；FAIL 就不能進 Studio/upload。Long-form 外部連結可點擊需要頻道 advanced features；Shorts description URL 不可點
- 所有新影音旁白必須使用 OpenAI Text-to-Speech，且只讀 `OPENAI_TEXT_TO_SPEECH_API_KEY`；預設每支影片從 `coral,nova,shimmer,sage` 隨機選一個女性感知識型旁白 voice，同一支影片內保持一致；TTS instructions 必須以自然台灣華語為目標，避開中國大陸普通話口音、兒化/重捲舌、央視/播音腔與中國在地用語。OpenAI TTS 口音控制是 prompt-guided，不是硬性的 `accent=tw` 開關，所以每支片上傳前要抽聽並寫 `review/tts-voice-check.md`；若明顯偏中國口音或用語，不問 Isaac，直接換更嚴格 instructions 或換 voice 重生、重 render，直到台灣觀眾可接受才 upload。每支旁白影片開頭保留 0.9 秒左右靜音畫面（可接受 0.8-1.1 秒），第一個真正說話起點目標 1.35-1.80 秒，低於 1.30 秒擋上傳；`review/audio-check.txt` 必須有人耳 `opening_word_spot_check=PASS`。每頁旁白轉場也要避免硬切：不要 aggressive `silenceremove`，不要 `atrim=start=...` 切 raw TTS 開頭，不要 voice fade-in；要在語音本體前插入 0.40-0.60 秒 pre-roll 空白、語音後加 0.30-0.50 秒 tail silence，並抽聽第一個字與前幾個 slide boundary，避免聲音突然冒出、破碎、click/pop 或剪到子音；final mix 要用 `scripts/check_video_loudness.py` 檢查，目標約 `-16 LUFS` integrated / `LRA 7` / `-1.5 dBTP`，低於 `-18 LUFS` 或 true peak 高於 `-1.0 dBTP` 要重做或用 `scripts/normalize_video_loudness.py` 正規化；缺 key/auth/quota/rate-limit 是 blocker，不要靜默改用 NotebookLM、macOS `say`、ElevenLabs/sag 或通用 `tts` 工具；不要再為影片跑 OpenAI billing/cost/usage snapshot，也不要在 final report 裡寫 billing unavailable，除非 Isaac 明確要求單次查詢
- 音訊 smoothing 的 pre-roll 是「在語音前插入空白聲音」，不是切音檔、不是把語音本體 fade 掉。新片不得用 `atrim=start=...` 追第一個字，也不得做 voice `afade=t=in`，不得讓第一段旁白 `preroll_ms=0`。每一段（含 slide 1）都要先插入至少 0.40 秒 pre-roll，再保留 tail silence；review 要檢查 build script / build-result / 抽聽，若第一個字或 slide boundary 子音被切，直接 FAIL，不准 upload。
- 上傳前必跑 `scripts/lint_audio_smoothing.py <artifact_dir> --json-out review/audio-smoothing-lint.json`，PASS 才能 upload；這個 linter 會抓 `silenceremove`、raw TTS `atrim=start=...`、pre-roll 低於 0.40 秒、第一段 pre-roll 為 0、以及 voice fade-in 的錯誤。
- 新片說明要預設觀眾不懂，不要假設聽的人知道名詞或背景。每個核心概念都要說清楚「是什麼、為什麼重要、怎麼運作、例子/情境、取捨或失敗模式、在哪一頁/哪段講清楚」。上傳前必寫 `review/explainer-comprehension.md`，並跑 `scripts/check_explainer_comprehension_record.py <artifact_dir> --json-out <artifact_dir>/review/explainer-comprehension-check.json`，FAIL 就重寫 slides/narration/review，不准 upload。概念密度高的技術長片還要跑 `scripts/check_concept_depth_gate.py <artifact_dir> --json-out <artifact_dir>/review/concept-depth-check.json`；若 FAIL 代表影片仍太壓縮、開場負擔太高、或受眾沒有明確包含非專門工程背景，不准 upload，先加長、拆概念、重寫開場或改成系列。開 Studio 前最後跑 `scripts/check_article_video_publish_ready.py <artifact_dir> --json-out <artifact_dir>/review/publish-ready-check.json`，只有 PASS 才能進公開上傳。
- 未發布新片若 audio smoothing lint、loudness、description lint、public-surface correctness、first-30-second clarity 等 deterministic gate FAIL，不要問 Isaac 要不要修；maintainer 要自動修、重 render、重跑 gate，直到 PASS 才 upload。只有已公開影片要改公開狀態（重傳 replacement、隱藏/下架舊片、重大改 metadata）才需要 Isaac 明確指示或既定 replacement policy。
- 背景音樂可做輕柔 pilot，但只用官方 YouTube Audio Library / Creator Music / 原創且權利清楚的來源；自動流程優先 `Attribution not required`。先選無人聲、soft/ambient/piano/acoustic/lo-fi，預設只放 lead-in、開頭 10-20 秒、轉場、outro，不要整段壓在密集講解下。若音樂進入旁白段，要 duck 到旁白下方約 20-25 dB 並做手機/吵雜環境抽聽；聽不清楚、變刺耳、像行銷 trailer、或 Content ID/授權不明就不用音樂。記錄 `review/music-license.md` 與 build-result。
- 影片前 30 秒必須有 context briefing：訪談/Podcast/影片要交代被訪談者是誰、角色/組織/專案、為何值得聽、這次訪談回答什麼問題；文章/論文/release/repo 要交代技術問題、位在 backend/AI workflow 哪裡、原本 baseline/status quo、為何現在重要
- 空檔補位排程會持續找新好來源；若已有影片長任務、來源分數不足、重複或找不到原始來源，就跳過並回報原因，不硬湊影片
- 小舟範圍是技術、AI、backend/software、工程 operator 訪談；不要刻意把其他題型拿來補量。
- 技術 AI/backend 是核心來源類別。AI 不要被硬性降量；改用來源多樣性與時間有效性取捨。若最近一直來自同一 domain、registry source、source family、arXiv/paper family、訪談頻道或 company blog，套用軟性重複降權，優先找同樣高品質但不同原始來源或不同 evidence type 的 AI/工程題。AI 題以 30 天作為 freshness baseline；超過 30 天要判斷內容是否已被新模型、新 API、新 benchmark、新價格或新產品節奏淘汰。時間敏感 AI 舊文要刷新或跳過，耐久的概念/架構/歷史/retrospective 題才可保留。Styling 題只作為少量旁支。
- YouTube Community/Blogger 也要假設讀者不懂：先說核心詞/來源物件是什麼，再說怎麼運作、來源結論與限制。YouTube Community source-intro 必須換行排版，建議 3-6 個短 block；`來源：` 和 raw URL 分兩行，YouTube note 也分行。Routine source-intro 不附 Blogger/Blogspot 連結，即使同題已有 Blogger 文章也不放；不要把 URL 接在標點或 `來源：` 後面。
- 困難技術題必須有 difficulty budget：列出 2-5 個 hard parts，最難概念至少用一張投影片或 30-60 秒旁白拆成白話問題、具體例子、術語定義、機制、取捨/失敗模式、可採用的判斷或 debug 流程；不可只翻譯名詞快速帶過
- 找題目時避開明顯替自家公司打廣告的行銷文、客戶案例、partner announcement、lead-gen 或「為什麼選我們」文章；只有具備機制、架構、benchmark、限制或實作/營運細節時才可選
- 自動 YouTube 影片沒有「最近剛發布就不能做」的 cooldown。兩小時只代表補位排程節奏，不是硬性 gate；排程時間到了就可以做。production/render/upload/Public 前仍要查同來源/同題重複、active render/upload/TTS/YouTube Studio 長任務、弱來源、review/account/browser blocker 與重複上傳風險
- YouTube 品質學習改成每 30 分鐘 sprint：封面、背景音樂/音效、開頭 30 秒 retention、視覺構圖、標題/description、旁白節奏、Shorts vs long-form、review gate。寫入 `reports/youtube-quality-learning/YYYY-MM-DD.md`，只有具體可重複的教訓才提升成規則
- 品質學習是最低保證，不是上限。若自動產片時段因來源不夠強、重複、找不到原始來源、active 產片/上傳衝突、或不適合公開而 skip/no-op，且沒有 active render/upload/TTS/YouTube Studio 長任務，就在同一輪續做品質學習，寫入 `reports/youtube-quality-learning/YYYY-MM-DD.md`；優先產出可落地規則、review checklist、封面/音樂/開頭改善或影片候選。若單一學習主題提前完成，且離下一個產片槽仍有 15 分鐘以上、沒有 active 產片/上傳資源衝突，就接著學下一個主題直到接近單輪時間上限。若有 active 產片/上傳或下一個產片槽 15 分鐘內開始，只做輕量閱讀/筆記或 skip，避免干擾正在進行的產片
- 品質學習需過白話 usefulness gate 才能套用：每個學習點要說清楚「解決我們哪個已觀察到的問題」、「怎麼小測」、「什麼結果算有效」、「有什麼風險」、「決定是 apply/test/watch/reject」。來源本身要標 priority：P0 本地證據/Isaac 回饋/artifact/analytics，P1 官方平台/政策/docs，P2 直接實驗/頻道數據，P3 可信創作者教育，P4 傳聞/growth hack/trend。只有低風險、可小測，且有 P0/P1/P2 支持，才升級規則；回報 Isaac 時不要只講證據等級，要講為什麼可能有用、怎麼知道有用
- 目前 TodayShip 觀眾量不足以把 YouTube analytics 當主要驗證來源；不要用「等 retention/CTR」當作延後修明顯品質問題的理由。早期以 Isaac 實際觀看體驗、使用情境（例如捷運聽不清楚）、本地 artifact/review gate、手機截圖/公開頁檢查、before/after 小測作為主要品質訊號。
- 新片 review 要產出 `review/pre-analytics-quality-scorecard.md`：檢查 Isaac viewing fit、audio comfort、mobile readability、one-second promise、first-30-second clarity、comprehension load、source traceability、pacing/usefulness。audio/source/first-30/public correctness FAIL 要擋 upload；WARN 要變成下一支片的具體改善。
- 品質學習過程可以形成影片候選，但學習 job 只寫候選、不上傳、不 Public。若本輪學習有來源理解價值，寫入 `reports/youtube-quality-learning/video-candidates/YYYY-MM-DD-<slug>/`，包含 outline、script draft、thumbnail notes、publish decision；只有正式影片時段或 Isaac 手動指定後，通過 source-understanding value/review/來源/dedupe/active-task/public-page verification gate 才能公開
- 影片製作前必須有 reader understanding value：目標觀眾、為何現在重要、來源問題、以及「看完後，觀眾可以更容易理解...」的具體來源理解價值。若只有「了解新聞/數字」或只剩特定角色用途，就拒絕或重塑題目；公開開頭不要列 PM/企業主/老師等角色清單。
- YouTube Community source-intro 發布前要跑 `scripts/check_youtube_community_source_intro_scope.py` 和 `scripts/check_youtube_community_source_intro_explainer.py`，兩者都 PASS 才能開 composer；scope gate 會確認來源是否落在技術/AI/backend/software/operator。Blogger 影片改寫文章發布前要跑 `scripts/check_blogger_explainer_article.py --require-youtube-url`。這些文字面也要有白話定義、機制/邊界、來源結論或限制與來源 URL，不准只發 teaser。
- 23:00-08:00 也可以產影片、render、review、upload、Public 發布；安靜時段只少發低價值進度碎訊，不代表跳過影片製作
- `codex exec` 若出現 `401 Unauthorized` / missing bearer/basic auth，是 runtime auth blocker；先檢查 `HOME=/Users/openclaw-user`、`CODEX_HOME=/Users/openclaw-user/.codex`、以及 `agent/codex-home/auth.json` 連到 `/Users/openclaw-user/.codex/auth.json`
- 23:00-08:00 若影片發布完成、no-op、或 Chrome/YouTube/OAuth/profile/upload 卡住，仍要回報；其他低價值狀態可等白天
- 每天最多一次檢查 Codex/OpenClaw runtime 升級與安全狀態：跑 `scripts/check_runtime_upgrades.py`。如果輸出不是 `NO_REPLY`，回報版本差異、安全 audit 結果、high/critical vulnerability 狀態、以及升級前 smoke-test 清單。這只產生 staged upgrade/security report；全域安裝、gateway/runtime 切換或預設模型變更前要有可驗證報告與明確升級步驟。
- 每天 AI interview/operator discovery 由 `ai-interview-source-discovery-daily-0850` cron 產生 `reports/source-discovery/YYYY-MM-DD.jsonl`；選題時把它當來源池外的 discovery context，強候選要追原始 YouTube/episode URL，反覆出現的高品質頻道再提升進固定 `SOURCE_REGISTRY.yml`。
- 每 2-3 天整理最近 `memory/*.md`，必要時更新 `MEMORY.md`
- 每週至少一次維護 technical source pool：檢查 `reports/source-watch/source-health.json`、最近 7 天 `reports/source-watch/*.jsonl`、近期影片 `source.json` / `review/improvement-notes.md`；跑 `scripts/build_weekly_tech_radar.py --date YYYY-MM-DD` 產生 `reports/weekly-tech-radar/YYYY-MM-DD.md`，用它回顧本週掃到什麼、為何選、哪些沒選、哪些可能沒掃到；修掉 fetch/parser 問題，對反覆薄、行銷、重複、不可驗證、或讓已發布內容過度集中在少數網站/source family 的來源降權或停用，對 coverage gap 補高品質 primary/project/engineering 來源；修改後用 clean temporary dry-run 驗證 registry parse、ID unique、fetch/parse、candidate quality。
